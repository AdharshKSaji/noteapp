import 'package:flutter/material.dart';

class ColorConstants {
  static const Color primaryBlue = Color(0xff3797EF);
  static const Color primaryWhite = Color(0xffFFFFFF);
  static const Color primarycolor = Color.fromRGBO(0, 0, 0, 255);
  static const Color primarybox = Color.fromRGBO(20, 28, 39, 255);

  static const Color primaryGreen = Colors.green;
  static const Color primaryyewllow = Colors.yellow;
  static const Color primaryRed = Colors.red;
  static const Color primaryGrey = Colors.grey;
  static const Color primaryBlack = Colors.black;
   static const Color primarypink = Color.fromARGB(255, 219, 156, 177);
}
